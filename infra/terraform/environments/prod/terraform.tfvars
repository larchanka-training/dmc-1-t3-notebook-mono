# Non-secret defaults for the production environment.
# api_image_uri is NOT set here — it is passed by CI at apply time.
# custom_domain is left empty until a domain is purchased.
custom_domain = ""
